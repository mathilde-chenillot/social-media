import { createTheme } from "@mui/material";

export const theme = createTheme({
    // where we store colors
    palette: {
        primary: {
            main: "#1760a5",
            light: "skyblue",
        },

        secondary: {
            main: "#15c630",
        },

        otherColor: {
            main: "#999",
        },
    }
})